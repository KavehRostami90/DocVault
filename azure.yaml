# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: docvault
metadata:
  template: docvault@0.0.1

services:
  api:
    project: src/DocVault.Api
    language: dotnet
    host: appservice

infra:
  provider: bicep
  path: infra
  module: main
